function soma(a, b){
    return a + b;
}

soma(2, 2);
soma(2, 3);
soma(2, 3);
soma(2, 4);
soma(2, 5);

// Currying é a técnica de transformar uma função com N parametros(n1,n2,n3,n4), em apenas uma função. Que recebe 1 parametro, e para cada parametro é retornado uma nova função.

// Currying no Javascript: é bem comum em linguagens funcionais. O javascript não é  por padrão uma linguagem funcional. Mas é possivel escrever código funcional e ir adaptando.